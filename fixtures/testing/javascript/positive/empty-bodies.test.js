// N2: Empty Test Bodies - VIOLATIONS

// Empty arrow function body
it('works', () => {});

test('also works', () => {});

// Empty with whitespace
it('empty with space', () => { });

test('empty multiline', () => {
});
